# Unraid

In Community Applications import `unraid/hh-media-guard.xml`, set an appdata path, and map media roots **read-only**. Match the container paths in Media Guard mappings to paths that actually exist in the container. Start it, visit the WebUI, and complete setup. Do not enable destructive actions; v0.1 does not support them.
