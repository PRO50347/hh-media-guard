# v0.2.0 delivery checklist

## Completed and validated foundations

- [x] v0.1 database data is preserved by additive migration ledger.
- [x] Local administrator bootstrap, hashed password, HttpOnly session, CSRF and exact-origin checks.
- [x] Encrypted write-only Arr API-key persistence with `ENCRYPTION_KEY` validation.
- [x] Monitor Only default and health/Docker baseline.
- [x] Typed Arr client foundations, mapped Arr enumeration, webhook HMAC/replay intake, leased scan-job foundation.

## Worker and audits

- [x] Fix worker error ownership: retry exactly the originally claimed job and never claim another job in a catch path.
- [x] Tests proving failed-job retry ownership, lease expiry, bounded retries, stale recovery, and duplicate prevention (`tests/worker.test.ts`, nine behavioral cases).
- [x] Bounded concurrency, graceful shutdown, cancellation, progress totals, and durable worker recovery (single-worker bound; 13 worker cases, prompt cancellation and fenced counts).
- [x] Sonarr library enumeration contract tests (file-ID joins, identity failures, season selection).
- [x] Radarr library enumeration contract tests (movie/file resource correlation).
- [x] Full-library/title/season/movie/file/rescan audit jobs.
- [x] Unchanged-file fingerprint deduplication (generated-media worker integration test).
- [x] Unmapped media becomes Needs Attention (queued imported-path integration test).

## Administration UI

- [x] Complete integration/settings UI with Sonarr and Radarr save/test/remove states.
- [x] Path-mapping add/edit/disable/delete/test UI.
- [x] Language-policy and safety-mode UI.
- [x] Branding names/theme/accent UI.
- [x] Secure branding uploads: logo, compact logo, favicon, artwork; MIME/content/size/path/XSS validation.
- [x] Suite links UI and persistence.
- [x] Movies UI, TV Shows UI, Jobs UI, History UI, and Needs Attention workflow.

## Workflow and remediation

- [x] End-to-end webhook → queued scan → persisted result test (`tests/webhooks.test.ts`, actual generated audio; per-integration bearer tokens, atomic receipt/job persistence and replay deduplication).
- [x] Quarantine operation validation and UI (production browser quarantine/restore workflow; exclusive movement, identity, cancellation and parent-swap regression tests).
- [x] Restore and cross-filesystem quarantine tests (`tests/quarantine.test.ts`: nine generated-fixture cases; collision refusal, dual safety gates, changed evidence, EXDEV copy and failed-copy preservation).
- [x] Automatic remediation using mocked Arr clients (`tests/remediation.test.ts`, generated fixtures for both sources).
- [x] Sonarr/Radarr replacement search and supported rejection/blocklist behavior (reconciled quarantine, failed-history rejection, one owned search).
- [x] Replacement verification (fresh passing evidence and changed, correlated Arr file identity).
- [x] Retry-loop prevention: per-release/title limits, cooldown, admin retry/reset/accept/ignore controls.
- [x] Destructive-operation idempotency and partial-failure handling (durable unique operations, uncertain search not repeated, quarantine/restore and runtime recovery tests).

## Detection, tests, and security

- [x] Optional smart-language detector interface and honest optional-sidecar documentation (`docs/interrupted-work-review.md`: interface only, no installed adapter/model or worker AI execution).
- [x] Substantially expanded unit tests and integration tests (19 files; metadata, jobs, remediation, quarantine, security, migration and backup fixtures).
- [x] Generated ffmpeg fixtures (English, Spanish, French, untagged, foreign main plus English commentary; no copyrighted media).
- [x] Playwright browser tests (isolated production Docker workflow, generated media/mock Arr, mobile/login/settings/branding/audit/quarantine/restore).
- [x] v0.1 → v0.2 migration fixture and backup/restore test (actual v0.1 schema preserves settings/scans/events/rejections; checked SQLite snapshot, branding, checksum failure and exclusive restore destination).
- [x] Authentication/session/CSRF/origin tests (`tests/auth.test.ts`: session hashing/expiration/revocation, bootstrap exclusivity, password checks, durable rate limiting, exact origins and CSRF).
- [x] SSRF/DNS-rebinding review, traversal/symlink review, webhook spoof/replay review.
- [x] API-key leakage, upload/XSS, SQLite/job-concurrency reviews.

## Documentation and release

- [x] Complete accurate setup, Docker, Unraid, integrations, mappings, branding, security, backup/rollback, detector, and developer documentation.
- [ ] Production build, Docker build/health smoke, non-root verification.
- [ ] Dependency audit, secret scan, `git diff --check`, clean status.
- [ ] GitHub CI and release workflow validation.
- [ ] Annotated `v0.2.0` tag, GitHub Release, and GHCR `0.2.0`, `0.2`, `0`, `latest` verification.

## v0.2 completion evidence

- Worker/recovery: 13 worker tests, cross-connection durable claim test, cancellation during Arr enumeration, fenced totals and immediate shutdown retry.
- UI/audits: expanded isolated production Playwright workflow passes; covers integration save/test/remove, mapping add/test/edit/disable/delete, Suite link persistence, branding slots/theme, full and series/season audits, media/history/jobs, attention ignore/reset and quarantine restore. Series IDs are no longer incorrectly used as episode filters; media rescans retain Arr identity.
- Remediation: both Arr contract mocks pass generated-media quarantine/reconcile/reject/search/verification; uncertain rejection/search cannot repeat, even after manual budget reset. Override unit tests prove title pause, reset and identity-preserving retries.
- Security: `docs/security-review.md`; transport tests verify mixed DNS rejection, pinned lookup, redirect refusal and response-body secrecy. Existing auth/path/upload/webhook/migration/backup/runtime tests remain enabled.
- Documentation: setup, Docker, Unraid, integration bearer tokens, branding, operations, backup/rollback, detector limitations and architecture now describe the shipped implementation.
