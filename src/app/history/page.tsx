import Link from 'next/link';import {listScans} from '@/lib/store'; export const dynamic='force-dynamic'; export default function History(){const scans=listScans();return <main className="simple"><Link href="/">← Dashboard</Link><h1>Scan history</h1><article className="card">{scans.map(s=><p key={s.fingerprint}><span className={'badge '+s.decision}>{s.decision}</span> {s.path} — {s.reason}</p>)||<p>No history.</p>}</article></main>}
