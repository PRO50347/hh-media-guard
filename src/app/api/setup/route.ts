import {NextResponse} from 'next/server';import {z} from 'zod';import {saveSettings} from '@/lib/store';const schema=z.object({suiteName:z.string().max(80).optional(),appName:z.string().max(80).optional(),shortName:z.string().max(40).optional(),accent:z.string().regex(/^#[0-9a-fA-F]{6}$/).optional(),setupComplete:z.boolean().optional(),safetyMode:z.literal('monitor').optional()});export async function POST(req:Request){const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid settings'},{status:400});return NextResponse.json(await saveSettings(parsed.data));}
