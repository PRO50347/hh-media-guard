import {describe,it,expect} from 'vitest';import {decideAudio} from '../src/lib/rules';import type {AudioTrack} from '../src/lib/types';const p={requiredLanguages:['eng'],allowDescriptive:false,ignoreCommentary:true,requireMainProgram:true,unknownBehavior:'needs-analysis' as const};const t=(language:string,extra:Partial<AudioTrack>={})=>({index:0,codec:'aac',language,isDefault:false,isCommentary:false,isDescriptive:false,...extra});
describe('audio policy',()=>{it('accepts full length English',()=>expect(decideAudio(600,[t('English',{duration:598})],p).decision).toBe('pass'));it('fails Spanish-only',()=>expect(decideAudio(600,[t('spa',{duration:600})],p).decision).toBe('fail'));it('rejects short English commentary',()=>expect(decideAudio(600,[t('spa',{duration:600}),t('eng',{duration:60,isCommentary:true})],p).decision).toBe('fail'));it('requires substantial duration even without title',()=>expect(decideAudio(600,[t('eng',{duration:30})],p).decision).toBe('fail'));it('flags unknown metadata',()=>expect(decideAudio(600,[t('und',{duration:600})],p).decision).toBe('needs-analysis'));it('accepts a valid second language track',()=>expect(decideAudio(600,[t('spa',{duration:600}),t('eng',{duration:590})],p).decision).toBe('pass'))});
